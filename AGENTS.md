# FileShare Project Notes

## 项目目标

这是一个局域网文件共享项目，包含两个使用面：

- 管理端：本机 Tauri 桌面 UI
- 客户端：通过 HTTP 访问的 Web 页面

核心模型如下：

- 管理端共享的是服务端本机文件，不复制文件内容
- 客户端上传文件到服务端当前用户的 `Downloads` 目录
- 管理端和客户端都可以发布文本
- 双端共享列表实时同步
- 文本只复制，不下载
- 文件可以下载

## 当前需求约束

- 默认端口为 `5421`
- 管理端先输入端口，再点击“启动服务”
- 启动后按钮文案切换为“停止分享”
- 管理端右上角展示客户端二维码和链接复制按钮
- 客户端页面也展示二维码和链接复制按钮
- 二维码和链接使用局域网 IP
- 管理端和客户端二维码都支持点击放大预览
- 客户端页面不暴露管理端入口
- 管理端能力只允许服务端本机访问，局域网其他设备不能调用管理操作

## 管理端行为

- 管理端是 Tauri 应用窗口，不是浏览器管理页面
- 管理端可以：
  - 启动服务
  - 停止服务
  - 发布文本
  - 使用系统文件选择器共享本机文件
  - 下载文件
  - 删除任意共享条目
  - 复制文本

- 管理端共享文件时：
  - 必须通过系统文件选择器选择
  - 只登记本机文件路径
  - 不复制到临时目录
  - 如果原文件被移动或删除，客户端下载会失败

## 客户端行为

- 客户端通过 `http://<LAN_IP>:<PORT>/client.html` 访问
- 客户端可以：
  - 上传文本
  - 上传一个或多个文件
  - 下载文件
  - 复制文本
  - 复制文件下载链接

- 客户端上传文件时：
  - 文件保存到服务端用户的 `Downloads` 目录
  - 尽量保留原始文件名
  - 如果重名，自动追加 ` (1)`、` (2)` 之类后缀

## 列表展示要求

- 页面主体是单列表布局
- 操作按钮放在列表区域右上角
- 文件和文本都展示在同一列表
- 文本不使用独立标题
- 文本列表项直接显示文本前一段内容，最长约 40 个字符
- 超过显示长度时追加 `...`
- 文件项显示文件名
- 文件项文件名后面带一个较小的 `🔗` 按钮，用于复制该文件的下载链接
- 管理端操作按钮为横向两个小按钮
- 客户端不显示删除按钮

## UI 约束

- 风格参考 Element UI / Element Plus 的干净后台样式
- 上传文本和上传/共享文件使用弹窗
- 客户端上传文件支持拖拽上传
- 选择文件后自动上传
- “上传文本/发布文本”和“上传文件/共享文件”按钮颜色区分
- 按钮带 emoji 图标
- 复制文件链接按钮点击后不改文案，只允许通过颜色变化给出反馈，避免布局抖动

## 技术结构

- `public/admin.html`
  - 管理端前端页面，由 Tauri 窗口加载

- `public/client.html`
  - 客户端 Web 页面

- `public/app.js`
  - 管理端/客户端共享前端逻辑
  - 包含列表渲染、SSE、文本发布、文件上传、下载、删除、复制文本、复制文件链接、二维码放大、Tauri 启停服务调用

- `public/styles.css`
  - 管理端与客户端共用样式

- `src-tauri/src/main.rs`
  - Tauri 入口
  - 暴露桌面命令，如启动服务、停止服务、系统文件选择器

- `src-tauri/src/server.rs`
  - Rust HTTP 服务
  - 提供客户端页面、接口、SSE、文件下载、客户端上传保存、管理端本机访问控制

- `data/items.json`
  - 共享列表元数据存储

## 接口语义

- `/api/items`
  - 获取共享列表

- `/api/events`
  - SSE 实时推送列表变化

- `/api/text`
  - 发布文本

- `/api/upload`
  - 客户端上传文件
  - 管理端不应走这个接口共享文件

- `/api/local-file`
  - 管理端登记本机文件路径为共享文件

- `/api/items/:id/download`
  - 下载文件

- `/api/items/:id`
  - 管理端删除共享条目

- `/api/share-info`
  - 返回客户端访问链接和二维码

- `/api/client-info`
  - 管理端获取客户端二维码和访问链接

## 本地安全边界

- 管理端相关接口必须校验请求来源是否为服务端本机地址
- 客户端只能做普通访问、上传文本、上传文件、下载文件、复制文本
- 这是一个面向可信局域网的工具，不包含登录、鉴权、HTTPS、病毒扫描或公网暴露能力

## 开发注意事项

- 这是纯 Tauri 项目，不再需要旧的 Node.js 独立服务端
- 前端静态资源由 Rust 服务提供
- `public/` 下的静态文件通过 Rust `include_str!` 内嵌进二进制
- 修改 `public/` 下文件后，通常需要重启 `npm run dev`，否则运行中的应用可能继续使用旧前端资源
- 管理端文件共享必须保持“本机路径映射”语义，不能回退成上传到临时目录
- 客户端上传必须保持“保存到服务端 Downloads”语义
