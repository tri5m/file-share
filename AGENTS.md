# FileShare Project Notes

## 项目目标

这是一个局域网文件共享项目，包含两个使用面和一个内嵌服务：

- 管理端：本机 Tauri 桌面 UI
- 客户端：通过 HTTP 访问的 Web 页面
- 内嵌服务：由管理端启动的 Rust HTTP 服务，负责静态页面、Web API、SSE、下载、上传保存和元数据持久化

核心模型如下：

- 管理端不是浏览器管理后台，而是 GUI 桌面客户端
- 管理端的启动/停止服务、系统文件选择器、管理端下载等桌面能力走 Tauri command
- 局域网 Web 客户端只能访问 Rust HTTP 服务暴露的客户端页面和普通客户端接口
- 管理端共享的是服务端本机文件，不复制文件内容
- 客户端上传文件到服务端当前用户的 `Downloads` 目录
- 管理端和客户端都可以发布文本
- 双端共享列表实时同步
- 文本只复制，不下载
- 文件可以下载

## 当前需求约束

- 默认端口为 `5421`
- 管理端先输入端口，再点击“启动服务”
- 启动后按钮文案切换为“停止分享”
- 管理端右上角展示客户端二维码、访问链接复制按钮；如果存在多个可用物理/真实网卡 IP，展示地址下拉选择
- 客户端页面也展示二维码和链接复制按钮
- 管理端二维码、展示链接、复制链接、复制文件下载链接都跟随当前选中的访问地址
- 客户端页面的展示链接、复制链接、复制文件下载链接必须使用浏览器当前 URL 的 origin，不能使用服务端默认渲染的 IP 覆盖当前访问地址
- 服务端枚举地址时应过滤回环、链路本地、虚拟网卡、隧道网卡、代理/VPN 类网卡，避免把不可用于局域网访问的地址展示给用户
- 管理端和客户端二维码都支持点击放大预览
- 客户端页面不暴露管理端入口
- 管理端能力只允许服务端本机访问，局域网其他设备不能调用管理操作
- 客户端上传文件 body 限制为 `10G`
- 管理端共享本机文件不做大小限制；下载接口不做业务大小限制，并支持 `Range`/`Accept-Ranges: bytes`
- 下载错误、缺失文件等服务端响应必须使用 UTF-8，避免移动端浏览器乱码
- macOS 桌面端需要提供菜单栏托盘图标
- Windows 桌面端也需要提供系统托盘图标，关闭窗口后保持后台运行，退出逻辑与 macOS 一致
- 点击窗口关闭按钮只隐藏窗口，不直接退出应用
- 关闭窗口后 Dock 中不保留应用图标，只在菜单栏托盘中保持运行
- 菜单栏托盘需要提供单个“启动分享/停止分享”切换入口，并同步窗口内启动状态
- 应用退出必须通过菜单栏托盘菜单中的“退出”
- Windows 系统托盘菜单需要在“检查更新”和“退出”之间提供“关于”，弹窗展示应用名、版本、简介、作者 `Trifolium Wang` 和 GitHub 地址
- macOS DMG 需要附带名为“损坏修复”的可执行文件，用于解除 `/Applications/FileShare.app` 的 quarantine 属性
- 桌面应用需要支持 Tauri updater 自动更新，系统托盘中提供“检查更新”入口
- updater 更新源使用 GitHub Release 中的 `latest-{{target}}-{{arch}}.json`
- updater 私钥不能提交到仓库；本地 `.updater/` 目录已加入 `.gitignore`

## 管理端行为

- 管理端是 Tauri 应用窗口，不是浏览器管理页面
- 管理端可以：
  - 启动服务
  - 停止服务
  - 发布文本
  - 使用系统文件选择器共享本机文件
  - 查看文件并在系统资源管理器/Finder 中定位
  - 移除任意共享条目
  - 复制文本
  - 从菜单栏托盘重新显示窗口或退出应用

- 管理端共享文件时：
  - 必须通过系统文件选择器选择
  - 只登记本机文件路径
  - 不复制到临时目录
  - 如果原文件被移动或删除，共享列表需要标记为失效；管理端“查看”按钮禁用或点击时弹窗提示，Web 端下载按钮禁用
  - 管理端列表中如果 Web 端正在下载文件，需要显示下载图标和速率，文字保持简洁，避免影响原有操作

## 客户端行为

- 客户端通过 `http://<LAN_IP>:<PORT>/client.html` 访问；当前服务端也把 `/` 路由到同一个客户端页面
- 客户端可以：
  - 上传文本
  - 上传一个或多个文件
  - 下载文件
  - 复制文本
  - 复制文件下载链接

- 客户端上传文件时：
  - 文件保存到服务端用户的 `Downloads` 目录
  - 尽量保留原始文件名
  - 如果重名，自动追加 ` (1)`、` (2)` 之类后缀
  - 移动端不依赖拖拽；点击选择文件后必须自动上传，不再要求二次点击确认

## 列表展示要求

- 页面主体是单列表布局
- 操作按钮放在列表区域右上角
- 文件和文本都展示在同一列表
- 文本不使用独立标题
- 文本列表项直接显示文本前一段内容，最长约 40 个字符
- 超过显示长度时追加 `...`
- 文件项显示文件名
- 文件项文件名后面带一个较小的 `🔗` 按钮，用于复制该文件的下载链接
- 管理端操作按钮为横向两个小按钮
- 管理端文件主操作是“查看”，用于在本机资源管理器/Finder 中定位原文件
- 管理端移除共享条目按钮文案为“移除”，不是“删除”，因为它只取消分享，不删除磁盘文件
- 客户端不显示移除按钮
- 失效文件需要灰色/删除线弱化展示；Web 端失效文件下载按钮禁用

## UI 约束

- 风格参考 Element UI / Element Plus 的干净后台样式
- 上传文本和上传/共享文件使用弹窗
- 客户端桌面浏览器可以支持拖拽上传，但移动端必须以点击选择文件为主
- 选择文件后自动上传
- “上传文本/发布文本”和“上传文件/共享文件”按钮颜色区分
- 按钮带 emoji 图标
- 复制文件链接按钮点击后不改文案，只允许通过颜色变化给出反馈，避免布局抖动
- 管理端启动分享按钮在未启动时居中显示并带呼吸效果；启动后缩小并移动到右下角，停止分享时反向过渡回中央
- 管理端多地址下拉是辅助控件，视觉上应保持小、轻、低优先级，不能压过二维码和访问链接
- 桌面端内容未超过视口时不应出现整体页面纵向滚动条；移动端保留自然页面滚动

## 技术结构

- `public/admin.html`
  - 管理端前端页面，由 Tauri 窗口加载

- `public/client.html`
  - 客户端 Web 页面

- `public/app.js`
  - 管理端/客户端共享前端逻辑
  - 包含列表渲染、SSE、下载状态 SSE、文本发布、文件上传、下载、移除、复制文本、复制文件链接、二维码放大、Tauri command 调用

- `public/styles.css`
  - 管理端与客户端共用样式

- `src-tauri/src/main.rs`
  - Tauri 入口
  - 暴露桌面命令，如启动服务、停止服务、服务状态、系统文件选择器、管理端下载、检查更新
  - 实现系统托盘、窗口关闭隐藏、退出和 updater 入口

- `src-tauri/src/server.rs`
  - Rust HTTP 服务
  - 提供客户端页面、HTTP 接口、SSE、下载状态 SSE、Range 文件下载、客户端上传保存、管理端本机访问控制、网卡地址枚举

- 共享列表元数据
  - 运行时保存在系统用户数据目录的 `FileShare/items.json`
  - 不是仓库内的 `data/items.json`

## Tauri Command 语义

- `start_server`
  - 管理端启动 Rust HTTP 服务
  - 返回客户端访问链接和二维码信息

- `stop_server`
  - 管理端停止 Rust HTTP 服务

- `server_status`
  - 管理端同步当前服务状态

- `set_preferred_port`
  - 记录托盘启动分享时使用的端口

- `pick_admin_files`
  - 管理端打开系统文件选择器，返回本机文件路径

- `download_admin_file`
  - 管理端通过系统保存面板把共享文件保存到用户选择的位置

- `reveal_admin_file`
  - 管理端检查共享文件是否仍存在；存在则在系统资源管理器/Finder 中定位，不存在则返回错误给前端弹窗提示

- `check_for_updates`
  - 管理端检查并安装 Tauri updater 更新

## HTTP 接口语义

- `/api/items`
  - 获取共享列表

- `/api/events`
  - SSE 实时推送列表变化

- `/api/download-events`
  - SSE 实时推送 Web 端下载状态，管理端用于展示正在下载和速率

- `/api/text`
  - 发布文本
  - `source=admin` 时必须来自服务端本机地址

- `/api/upload`
  - 客户端上传文件
  - 管理端不应走这个接口共享文件
  - 请求 body 限制为 `10G`

- `/api/local-file`
  - 管理端登记本机文件路径为共享文件
  - 只允许服务端本机地址访问

- `/api/items/:id/download`
  - 下载文件
  - 支持 `Range` 请求，响应主动包含 `Accept-Ranges: bytes`
  - 文件不存在时返回 UTF-8 错误响应

- `/api/items/:id`
  - 管理端移除共享条目
  - 只允许服务端本机地址访问

- `/api/share-info`
  - 返回客户端访问链接、二维码和可用访问地址列表
  - Web 客户端页面使用

- `/api/client-info`
  - 本机管理信息接口，只允许服务端本机地址访问
  - 当前前端管理端不直接依赖它，管理端主要使用 Tauri command 返回的服务信息

## 本地安全边界

- 管理端相关接口必须校验请求来源是否为服务端本机地址
- 客户端只能做普通访问、上传文本、上传文件、下载文件、复制文本
- 管理端共享文件不走 HTTP 上传接口，避免引入不必要的大小限制和复制语义
- 这是一个面向可信局域网的工具，不包含登录、鉴权、HTTPS、病毒扫描或公网暴露能力

## 开发注意事项

- 这是纯 Tauri 项目，不再需要旧的 Node.js 独立服务端
- 前端静态资源由 Rust 服务提供
- `public/` 下的静态文件通过 Rust `include_str!` 内嵌进二进制
- 修改 `public/` 下文件后，通常需要重启 `npm run dev`，否则运行中的应用可能继续使用旧前端资源
- 管理端文件共享必须保持“本机路径映射”语义，不能回退成上传到临时目录
- 客户端上传必须保持“保存到服务端 Downloads”语义
